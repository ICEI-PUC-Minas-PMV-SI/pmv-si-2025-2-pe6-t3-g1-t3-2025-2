#############################################
# 🌱 AMBIENTE DE DESENVOLVIMENTO (Vite + React)
# Este arquivo contém variáveis utilizadas
# apenas durante a execução local do projeto.
# Ele NÃO é carregado em produção.
#############################################

# 🏥 ENDEREÇO DA API (.NET)
# Utilize exatamente a porta exibida no terminal ao rodar "dotnet run".
# Exemplo: Now listening on: http://localhost:5288
VITE_API_BASE_URL=http://localhost:5288
# Se preferir usar HTTPS (mais seguro), descomente a linha abaixo:
# VITE_API_BASE_URL=https://localhost:7240

# 🔐 ENDPOINT DE LOGIN
# Caminho do endpoint responsável pela autenticação na API.
VITE_LOGIN_PATH=/api/Auth/login

#############################################
# ⚙️ OUTRAS CONFIGURAÇÕES OPCIONAIS
#############################################

# Porta usada pelo front-end (informativo)
VITE_FRONT_PORT=5173

# Nome da aplicação (usado em títulos, cabeçalhos, etc.)
VITE_APP_NAME=Hotel Fazenda Web

# Ambiente atual (padrão: development)
VITE_ENVIRONMENT=development

#############################################
# 📌 OBSERVAÇÕES IMPORTANTES:
# - NÃO coloque aspas nas URLs.
# - Sempre reinicie o servidor React após editar este arquivo:
#     (Ctrl + C no terminal e depois: npm run dev)
# - Para produção, crie o arquivo ".env.production"
#     com o endereço da API publicada no servidor real.
#############################################
