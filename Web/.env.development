#############################################
# Ambiente de Desenvolvimento (Vite + React)
# Este arquivo contém variáveis usadas
# apenas quando o projeto é executado em modo DEV.
# Ele não é carregado em produção.
#############################################

# 🏥 ENDEREÇO DA API (.NET)
# Aqui informamos o endereço base da sua API back-end.
# O React usará essa URL para enviar requisições (login, cadastros, etc).
# Use o mesmo endereço que aparece quando a API sobe no navegador (Swagger).
# Exemplo padrão do seu projeto:
VITE_API_BASE_URL=http://localhost:5210
# Se preferir usar HTTPS (mais seguro), substitua pela linha abaixo:
# VITE_API_BASE_URL=https://localhost:7237


#############################################
# ⚙️ OUTRAS CONFIGURAÇÕES OPCIONAIS
# Você pode criar variáveis adicionais aqui caso precise depois.
# Todas as variáveis devem começar com o prefixo "VITE_"
# para que o Vite permita o acesso dentro do código React.
#############################################

# 🔐 Exemplo: Porta padrão usada pelo front-end (apenas informativa)
VITE_FRONT_PORT=5173

# 🧾 Exemplo: Nome da aplicação (apenas para exibir em títulos, etc)
VITE_APP_NAME="Hotel Fazenda Web"

# 👩‍💻 Exemplo: Ambiente atual
VITE_ENVIRONMENT=development

#############################################
# 📌 OBSERVAÇÕES IMPORTANTES:
# - Não coloque aspas nas URLs.
# - Sempre reinicie o servidor React após editar este arquivo.
#   (Ctrl + C no terminal e depois: npm run dev)
# - Para produção, use outro arquivo chamado ".env.production"
#############################################
